#include<bits/stdc++.h>
using namespace std;
int main(){
    int i,n;

   for(int i=0;i<n/2;i++)        // O(N/2) = O(N) 
    {
        for(int j=1; j+n/2<=n; j++)    //  O(N/2) = O(N)
        {
            for(int k=1;k<=k;k*=2)        // O(logN)
            {
                cout << "meem";
            }
        }
    }

 //  O(N) * O(N) * O(logN) = O(N*N*logN)

}