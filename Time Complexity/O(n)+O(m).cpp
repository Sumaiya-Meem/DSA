#include<bits/stdc++.h>
using namespace std;
int main(){
  int N,M,x,y;
 for(int i=0;i<N;i++)     // O(N)
    {
        x++;
    }
    for(int i=0;i<M;i++)    //  O(M)
    {
        y++;
    }
     // O(N) + O(M)  
}